// Returns preview - component 
// 1. Idea -> Create markup in HTML, then use that to create a component for all sections. Divide into two possible return values: Experience and Education.
// SideNote -> Decide where user added fields will go.